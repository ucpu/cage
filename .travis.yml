language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - xorg-dev
      - nasm

matrix:
  exclude:
    - compiler: gcc
  include:
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
      env: COMPILER=g++-5 COMPILERC=gcc-5
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env: COMPILER=g++-6 COMPILERC=gcc-6

script:
  - export CXX=$COMPILER
  - export CC=$COMPILERC
  - mkdir build && cd build
  - cmake -DCMAKE_BUILD_TYPE=RELWITHDEBINFO ..
  - cmake --build .
  - result/relwithdebinfo/cage-test-core
